/**
 * Корневой конфиг Vitest — существует РАДИ ОДНОГО: детерминированного обнаружения тестов.
 *
 * Пакеты и приложения гоняют тесты своими конфигами (`apps/api/vitest.config.ts` и т.д.) через
 * turbo. Этот файл влияет только на прогоны ИЗ КОРНЯ — прежде всего на `pnpm test:arch`, который
 * запускает `vitest run tests/arch/*.spec.ts` без `--config`, то есть до сих пор шёл на дефолтах.
 *
 * **Что чинится.** Аргументы `vitest run <путь>` — это ФИЛЬТРЫ подстрокой, а не список файлов, и
 * применяются они ко всему, что найдено под `root`. В репозитории лежит зарегистрированный
 * git-worktree `.claude/worktrees/<...>` на СТАРОМ коммите — полная копия дерева со своими
 * `tests/arch/*.spec.ts`. Она подходила под те же фильтры и прогонялась наравне с настоящим кодом:
 * `.gitignore` на обнаружение тестов не влияет, он про git, а не про vitest.
 *
 * Наблюдаемое следствие: число архитектурных тестов в одном и том же гейте прыгало между
 * прогонами — 104, потом 105, потом 71, — потому что часть результатов относилась к устаревшей
 * копии кода, которую никто не редактирует. Гейт, чья измеряемая поверхность молча меняется от
 * запуска к запуску, не даёт сигнала: зелёный в нём может означать «старая копия цела».
 *
 * Каталоги локального состояния агентов (`.claude/`, `.serena/`) исключены целиком — они уже в
 * `.gitignore` как локальное состояние, и кодом проекта не являются по определению.
 */
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    exclude: [
      '**/node_modules/**',
      '**/dist/**',
      // Локальное состояние агентов, не код проекта. Здесь же живут git-worktree'ы —
      // копии дерева на других коммитах (см. JSDoc выше).
      '**/.claude/**',
      '**/.serena/**',
    ],
  },
})
