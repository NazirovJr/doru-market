/**
 * Stub seed-скрипт (DTJ-098, EP-04). Реальный seed с ≥300 курированных позиций
 * заполняется ниже (см. `seed-catalog.ts` в этом же файле). Этот stub — для
 * существующей команды `pnpm db:seed` в `apps/api/package.json` (изначально ссылался
 * на `src/infrastructure/database/seed.ts`, не существующий — здесь создаётся
 * реальный путь).
 */
export { runSeedCatalog } from './seed-catalog.run.js'
