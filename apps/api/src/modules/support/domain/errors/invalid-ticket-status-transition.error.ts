/**
 * Ре-экспорт `InvalidTicketStatusTransitionError` (`@dorutj/contracts`,
 * `domain-errors-support.ts`, DTJ-282) — тот же приём/причина, что `ticket-not-found.error.ts`
 * этого же каталога.
 */
export { InvalidTicketStatusTransitionError } from '@dorutj/contracts'
