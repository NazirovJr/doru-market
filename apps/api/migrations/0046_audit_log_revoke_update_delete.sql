-- =====================================================================================
-- 0046_audit_log_revoke_update_delete.sql — EP-16 (DTJ-374), SRS-ADM-064/SRS-DB-024:
-- REVOKE UPDATE, DELETE ON audit_log FROM app_role — эксплуатационный (не только
-- архитектурный) уровень неизменяемости audit_log.
-- =====================================================================================
-- Номер сверен перед созданием (правило 11 AGENTS.md): `ls apps/api/migrations` и
-- `meta/_journal.json` — последняя запись idx=47 `0045_support_ticket_last_escalated_at`,
-- следующий свободный — 0046.
--
-- НАЙДЕНО ПРИ РЕАЛИЗАЦИИ (не домысел тикета DTJ-374, который описывает это как ещё не
-- сделанное): буквально требуемый «Что сделать» п.1 REVOKE УЖЕ применён —
-- `0034_support_tickets_audit_log.sql` строка 135 (`REVOKE UPDATE, DELETE ON audit_log
-- FROM app_role;`) — потому что `CREATE TABLE audit_log` физически приземлилось РАНЬШЕ
-- этого тикета, побочным продуктом DTJ-247 (EscrowReconciliationJob, EP-10, apps/worker),
-- которому append-only журнал понадобился для записи ledger-расхождений задолго до волны
-- EP-16. Сама `0034` цитирует `0031_app_role_privileges.sql` строки 24-29, где авторы 0031
-- уже предвидели этот порядок («audit_log ЕЩЁ НЕ СУЩЕСТВУЕТ, придёт с EP-16») и явно обязали
-- миграцию, создающую таблицу, сразу выполнить именно этот REVOKE.
--
-- Этот файл тем не менее НЕ удаляет и не переписывает задним числом `0034` (диск — истина,
-- чужую миграцию не трогаем) — тот же приём, что `0030_escrow_ledger_grants.sql` уже
-- зафиксировал для ПАРНОГО случая `escrow_ledger` (REVOKE там тоже продублирован между 0030
-- и 0031, см. её шапку). Он существует как КАНОНИЧЕСКОЕ место операционного контроля
-- неизменяемости `audit_log`, принадлежащее правильному владельцу — `files_owned` DTJ-374,
-- SRS-DB-024. После применения ОБЕИХ миграций (0034, затем эта) по порядку это ПОВТОРНОЕ,
-- дважды подтверждающее REVOKE — безопасный no-op (правило 11 AGENTS.md: REVOKE идемпотентен
-- нативно, повторный REVOKE непривилегии не бросает — тот же вывод уже задокументирован
-- `0034` строка 46).
--
-- Обычная транзакционная миграция — не ALTER TYPE, SRS-DB-009/`disable-transaction`
-- неприменимы (тот же вывод — «Что сделать» п.1 тикета DTJ-374).
-- =====================================================================================

REVOKE UPDATE, DELETE ON audit_log FROM app_role;
