-- =============================================================================
-- 0010_otp_purpose_add_onboarding_contact.sql — EP-03 (DTJ-063)
-- =============================================================================
-- [ДОПОЛНЕНИЕ] `otp_purpose` enum: новое значение 'onboarding_contact'
-- (SRS-ADM-004 — OTP-верификация контактного телефона заявки онбординга).
-- Должно выполняться ВНЕ транзакции (SRS-DB-009).
--
-- КООРДИНАЦИЯ: `otp_purpose` формально владение EP-01 (модуль `auth`).
-- Значение добавляется ЭТИМ тикетом, т.к. его единственный потребитель —
-- onboarding contact phone verification (DTJ-064), который в свою очередь
-- нужен до завершения EP-03. Владелец EP-01 уведомлён о расширении
-- существующего типа (см. DTJ-063 «Риски»).
-- =============================================================================
-- disable-transaction

ALTER TYPE otp_purpose ADD VALUE IF NOT EXISTS 'onboarding_contact';
