-- Down-миграция для 0054_audit_action_category_add_role_grant.sql (DTJ-354).
-- НАМЕРЕННЫЙ NO-OP — см. 0043_audit_action_category_cash_reconciliation.down.sql: PostgreSQL
-- не предоставляет `ALTER TYPE ... DROP VALUE` ни в одной версии ядра, откат добавленного
-- значения ENUM штатными средствами невозможен без пересоздания типа и переноса всех
-- зависимых колонок. Значение остаётся в типе после отката — безвредно (неиспользуемый
-- допустимый вариант), тот же вывод, что уже зафиксирован для 0010/0043.
SELECT 1;
