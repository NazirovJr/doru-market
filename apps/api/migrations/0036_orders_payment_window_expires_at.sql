-- =====================================================================================
-- 0036_orders_payment_window_expires_at.sql — EP-10, DTJ-253 (UnpaidOrderTimeoutJob).
-- =====================================================================================
-- Номер: следующий свободный по `ls apps/api/migrations/` на момент создания файла —
-- занято по `0035_escrow_ledger_refund_unique.sql` (этот же исполнитель, DTJ-245) включительно.
--
-- Тикет DTJ-253 предписывал добавить эту колонку ЛИБО дописав в существующую эволюционную
-- миграцию DTJ-228 (`0020_orders_payments_extensions.sql` в тексте тикета — устарело, реально
-- `0028_orders_payments_extensions.sql`), ЛИБО отдельной следующей по номеру миграцией, «решить
-- по факту состояния кодовой базы» (буквальный текст тикета «Что сделать» п.1). Состояние на
-- момент этого файла: `0028_orders_payments_extensions.sql` УЖЕ ПРИМЕНЕНА (проверено —
-- `billing_strategy` присутствует в БД) — миграции в этом проекте неизменяемы после применения
-- (drizzle-миграции идентифицируются по содержимому/журналу; правка уже применённого файла не
-- переиграла бы её на живой базе и нарушила бы целостность журнала). Решение: ОТДЕЛЬНАЯ
-- следующая по номеру миграция, не правка `0028`.
--
-- Владелец ЗАПИСИ (кто пишет непустое значение при создании non-cash заказа) —
-- `CheckoutUseCase`/`Order.create()` (EP-09, `orders`-модуль, файлы `checkout.use-case.ts`/
-- `order.entity.ts`) — ВНЕ владения ЭТОГО тикета (DTJ-253, EP-10), буквальный текст «Что
-- сделать» п.3: «этот тикет ТОЛЬКО читает поле, не устанавливает его». ЗАФИКСИРОВАНО ЯВНО (DoD
-- тикета): на момент этой миграции НИКТО не пишет это поле — `UnpaidOrderTimeoutJob` находит
-- ноль строк в реальном трафике R1, пока отдельное действие (вне периметра DTJ-253) не добавит
-- запись в `CheckoutUseCase`. См. отчёт сдачи, разделы БЛОКЕРЫ/ДОПУЩЕНИЯ.
--
-- Аддитивно: `TIMESTAMPTZ NULL`, без `DEFAULT`, без backfill — откат тривиален (`.down.sql`).
-- =====================================================================================
ALTER TABLE orders
    ADD COLUMN IF NOT EXISTS payment_window_expires_at TIMESTAMPTZ NULL;

COMMENT ON COLUMN orders.payment_window_expires_at IS
    'DTJ-253, SRS-ORD-032/033/034: момент истечения окна на оплату non-cash заказа в '
    'pending_payment. Устанавливается ТОЛЬКО non-cash заказам при Order.create() '
    '(ответственность CheckoutUseCase/orders-модуля, EP-09, ВНЕ владения EP-10/DTJ-253) — '
    'NULL для cash_courier (никогда не проходит через pending_payment, D-25) и для любого '
    'заказа, созданного ДО этой миграции. UnpaidOrderTimeoutJob (apps/worker) читает поле, '
    'не пишет его.';
