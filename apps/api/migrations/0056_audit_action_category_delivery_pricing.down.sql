-- Down-миграция для 0056_audit_action_category_delivery_pricing.sql (EP-13, DTJ-322).
-- НАМЕРЕННЫЙ NO-OP: PostgreSQL не поддерживает `ALTER TYPE ... DROP VALUE` — откат добавленного
-- значения ENUM невозможен штатными средствами без пересоздания типа и переноса зависимых
-- колонок (audit_log.category с чужими данными). Значение остаётся в типе после отката —
-- безвредно, тот же вывод, что уже зафиксирован для 0010/0043/0054.
SELECT 1;
