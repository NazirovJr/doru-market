-- =============================================================================
-- 0009_verification_status_add_revoked.sql — EP-03 (DTJ-063)
-- =============================================================================
-- [ДОПОЛНЕНИЕ] `verification_status` enum: новое значение 'revoked' (SRS-ADM-017,
-- отзыв верификации). Должно выполняться ВНЕ транзакции (SRS-DB-009):
-- PostgreSQL запрещает использовать новое значение enum в той же транзакции,
-- где оно добавлено.
-- =============================================================================
-- disable-transaction

ALTER TYPE verification_status ADD VALUE IF NOT EXISTS 'revoked';
