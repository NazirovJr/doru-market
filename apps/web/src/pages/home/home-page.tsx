/**
 * `home-page.tsx` (DTJ-192, `SRS-CAT-001`).
 *
 * Главный экран Customer Web. `SRS-CAT-001` фиксирует порядок вёрстки первого экрана: строка
 * поиска — ПЕРВЫЙ содержательный элемент (не карта, не баннер доставки). Лента «дешевле рядом»
 * (топ-N товаров с наибольшей относительной экономией, «при наличии геолокации») зависит от
 * Analog Engine (EP-07, расчёт экономии) — явно ВНЕ объёма этого тикета (DTJ-192, «Риски»:
 * «сам виджет ленты экономии на главном экране — зона EP-07/интеграционная точка на `pages/home`,
 * НЕ входит в объём этого тикета»). Композиция сюда добавится отдельным тикетом EP-07, когда
 * бэкенд подбора аналогов будет готов — каркас страницы уже держит `SearchBar` первым блоком.
 *
 * Заменяет `app/routes/coming-soon-page.tsx` на корневом маршруте (`router.tsx`) — тот файл был
 * явным временным плейсхолдером до готовности реального поиска (см. его собственный JSDoc:
 * «реальный каталог/поиск не входит в DTJ-003»).
 */
import type { ReactElement } from 'react'
import { SearchBar } from '@/features/search/ui/search-bar'

const HomePage = (): ReactElement => (
  <section className="flex flex-col gap-4" data-testid="home-page">
    <SearchBar />
  </section>
)

export default HomePage
