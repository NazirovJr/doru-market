# Шаблон задания для qwen3-coder (исполнитель) — v1

> Для кого: координатор, который готовит задание локальной модели (qwen3-coder:30b в dsh).
> Почему он такой: каждый блок закрывает ошибку из `ANALYSIS-2026-09-19.md` (номер в скобках).
> Готовые задания по этому шаблону: `tasks/DTJ-307a.md` … `tasks/DTJ-307d.md` — все четыре
> применены координатором дословно и прошли гейт (ANALYSIS §3).

## Уровни детализации — с какого начинать

| Уровень | Что даёт координатор | Что делает модель | Когда переходить выше |
|---|---|---|---|
| 1 (сейчас) | Полный текст новых файлов и точные пары НАЙДИ → ЗАМЕНИ для правок | Применяет, пишет тесты по списку кейсов, подключает, проходит гейт | 3–5 заданий подряд: первый же гейт PASS, нет выхода за периметр, нет лжи в отчёте |
| 2 | Сигнатуры, образец того же вида, пары НАЙДИ → ЗАМЕНИ только для чужих файлов | Пишет тела методов и тесты | Снова 3–5 чистых заданий |
| 3 | Тикет, факты «есть / нет / ловушки», список файлов | Проектирует внутри файлов сама | — |

На первом уровне модель почти не проектирует, и это нормально: у qwen3-coder:30b ~3B активных
параметров, а проверенная ошибка DTJ-306 — именно проектирование без фактов. Переход выше
решается по таблице метрик (ANALYSIS §7), а не по ощущению.

На этапе калибровки (первые 3–5 заданий) координатор сам прогоняет образцы из раздела 3 через гейт
до отправки. Ошибка в образце иначе превращается в «ошибку модели».

## Чек-лист координатора — до отправки

- [ ] Среда настроена: модель `qwen3-coder-agent` (`num_ctx 131072`), тот же `contextWindow` в
      `~/.dsh/settings.yaml`, калибровочный прогон
      гейта внутри dsh пройден (ANALYSIS §4.1, §4.6).
- [ ] Задание не больше 8 файлов и одного процесса (api **или** worker). Больше — режь на a/b/c.
- [ ] Все архитектурные решения приняты тобой. В задании нет ни одного «на усмотрение
      разработчика».
- [ ] Каждый факт из раздела 2 проверен grep-ом или чтением кода **сейчас**, на том коммите,
      от которого пойдёт ветка. Записан хэш (ANALYSIS §3).
- [ ] Для каждого создаваемого файла в разделе 3 есть образец **того же вида** (контроллер →
      контроллер, спек → спек), код без длинных JSDoc (№8).
- [ ] Перечислены все места, которые ломаются от изменения: моки расширяемого порта, реестры
      вариантов union, позиционные вызовы конструкторов в спеках (№11).
- [ ] Раздел «Этого НЕТ» заполнен: поля, методы, классы и файлы, которые модель захочет
      выдумать (№6, №7).
- [ ] Все команды — PowerShell 5.1: без `&&`, `grep`, `find`. Проверки только через гейт (№3).
- [ ] `git status --short` в рабочей копии пустой, и на время задания в ней пишет только эта модель:
      ни ты, ни IDE, ни локальный стенд, ни вторая модель (№13). Ветку модель создаёт сама, первым шагом.
- [ ] В той же рабочей копии запущен `scripts/qwen-gate-daemon.ps1`: без него vitest в песочнице
      dsh не запускается (ANALYSIS §4.6). Для задания открыта новая сессия dsh.
- [ ] Сессия создана на пресете `executor` (фишка рядом с выбором рабочей папки на экране новой
      сессии; в шапке сессии видно, какой пресет запущен). В нём у модели нет `update_goal` —
      закрыть цель и объявить себя готовой она не может (ANALYSIS §4.9).
- [ ] `num_ctx` модели (`ollama ps`, колонка CONTEXT) равен её `contextWindow` в `~/.dsh/settings.yaml`.
      qwen3-coder-agent (30B) — 65536: 131072 на этой машине вдвое медленнее, KV-кэш выдавливает
      веса из видеопамяти (ANALYSIS §4.8). qwen3-coder-next-agent — 131072: полное внимание у неё
      в 12 слоях из 48, KV на всё окно около 1,7 ГБ, а сжатие истории отодвигается вдвое (§4.12).
- [ ] У провайдера `ollama` в `~/.dsh/settings.yaml` стоит `streamIdleTimeoutMs: 1200000`.
      С дефолтными 300 000 обрывается любой вызов инструмента длиннее ~1400 токенов (ANALYSIS §4.8).
- [ ] В шаблон подставлены `{{BRANCH}}` и `{{COMMIT_MSG}}` — модель не придумывает их сама.
- [ ] Команды проверки и сдачи стоят в шапке, в первых 3500 символах файла. Под давлением контекста
      dsh оставляет от длинного результата `read` только первые 4096 и последние 1024 символа —
      на DTJ-372 команда гейта оказалась в вырезанной середине, и модель выдумала свою (№34).
      `scripts/qwen-task.ps1` отказывается ставить такое задание активным.
- [ ] В разделе 0 подставлено число шагов раздела 4 (`{{ШАГОВ}}`): модель переписывает план
      своими словами и молча теряет последние пункты — на DTJ-307b так пропали гейт и сдача (№27).
- [ ] Рядом с заданием лежит `<id>.require.txt`: каждый критерий раздела 5 — отдельной строкой
      `путь::текст`, причём текст такой, что написать его, не написав тест, нельзя (№26).
      Проверь файл на готовом коде: гейт должен показать `[OK] require (N)`.
      `{{COMMIT_MSG}}` уходит в `-Commit`: без кавычек, обратных апострофов и знака доллара,
      до 200 символов, иначе демон отклонит запрос.
- [ ] Задание закоммичено в `development` (иначе модель не увидит его в своей ветке). В конце есть маркер `=== КОНЕЦ ЗАДАНИЯ <id> ===` (№1).

## Как отправлять в dsh

Одна строка, без вставки текста задания (защита от вставки не того текста, №1). Лучше — как цель
(`/goal`): если модель остановится посреди работы, dsh сам запустит следующий раунд.

```
/goal Выполни задание из файла reports/qwen3/tasks/<ID>.md: прочитай его инструментом read целиком. Не пиши текст перед вызовом инструмента: сразу вызывай инструмент.
```

Цель закрывает человек. На пресете `executor` инструмента `update_goal` у модели нет, поэтому
драйвер будет гонять раунд за раундом, пока цель активна; раундов не больше двадцати
(`defaultMaxGoalRounds` в `~/.dsh/profiles/web/cordis.patch.yml`). Увидел отчёт со строкой
`КОММИТ:` — закрой цель сам (`/goal clear`) или останови сессию. Повторный прогон гейта после
удачного коммита безопасен: он печатает тот же `КОММИТ:` и `GATE: PASS`, а не ошибку.

Фраза про текст перед вызовом обязательна. Без неё qwen3-coder примерно в каждом 5–8-м ответе пишет
вступление, а за ним вызов без открывающего `<tool_call>`. Ollama не распознаёт такой вызов, и ход
обрывается. С этой фразой — 8 чистых вызовов из 8, английская формулировка не помогает
(ANALYSIS §4.7).

Лимит раундов цели в dsh — 256. Если модель зациклилась, останови её командой `/goal pause`.
Без цели можно отправить ту же строку без `/goal`, но тогда любой обрыв останавливает работу.

После ответа модели приёмка всегда на координаторе: гейт `-Full` своими руками, ревью диффа её ветки,
`pnpm verify`, интеграционные спеки, затем мёрж в `development`. Отчёт модели — не доказательство (№14).

## Правила нарезки

- Один процесс: `apps/api` **или** `apps/worker`. Межпроцессный мост (HTTP-эндпоинт + клиент в
  worker) — две задачи: сначала эндпоинт, потом клиент.
- Порядок частей: контракты и типы → домен → application → infrastructure → presentation →
  регистрация в модуле → тесты.
- Интеграционные спеки с Postgres и Redis пишет и гоняет координатор. Гейт их пропускает.
- Время на задание: ориентир 40–60 шагов. Если модель подходит к 80 без `GATE: PASS` — остановить
  и разбирать, а не ждать.

## Шаблон

Всё между линиями — текст файла задания. `{{…}}` заменить. Разделы 1, 6, 7 и 8 одинаковы для
всех заданий: меняются только `{{GATE_ALLOWED}}` и `{{KNOWN_RED}}`.

````markdown
# ЗАДАНИЕ {{ID}} — {{КРАТКО_ЧТО_ДЕЛАЕМ}}

> Прочитай этот файл целиком одним вызовом `read`. Инструмент сам печатает в конце вывода
> `(End of file - total N lines)` — эта пометка и значит, что файл прочитан весь;
> последняя строка файла — `=== КОНЕЦ ЗАДАНИЯ {{ID}} ===`. Пометки нет — дочитай
> остаток через `read` с `offset` и только потом работай.
> Если история сжата или ты не помнишь следующий шаг — перечитай этот файл: `{{ПУТЬ_К_ЭТОМУ_ФАЙЛУ}}`.
>
> **Проверка и сдача — только этими двумя командами**, других способов нет. Они стоят в начале
> намеренно: когда история разрастается, dsh вырезает середину длинных результатов `read`, а начало
> оставляет. Видишь в выводе `[... tool result middle pruned ...]` — середина вырезана: нужный
> раздел перечитай через `read` с `offset`/`limit`. В вызове `pwsh` — всегда `timeoutMs: 600000`.
>
> Проверка:
> `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/qwen-gate.ps1 -Allowed {{GATE_ALLOWED}} -RequireFile reports/qwen3/tasks/{{ID}}.require.txt`
>
> Сдача (раздел 8) — та же команда с `-Commit`:
> `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/qwen-gate.ps1 -Allowed {{GATE_ALLOWED}} -RequireFile reports/qwen3/tasks/{{ID}}.require.txt -Commit "{{COMMIT_MSG}}"`

## 0. Карточка задачи

Цель: {{ОДНО_ПРЕДЛОЖЕНИЕ}}.
Рабочая папка — та, что открыта в этой сессии dsh (общий репозиторий, в нём работают и другие).
Меняешь ровно эти файлы:

СОЗДАТЬ:
- `{{путь}}` — {{что внутри}}

ИЗМЕНИТЬ:
- `{{путь}}` — {{что именно, одна-две строки}}

Любой другой файл не трогай. Понадобилось — это СТОП (раздел 7).
Не делаешь: {{ВНЕ_ОБЪЁМА}}.

Первое действие — `todo_write`. Первый пункт — цель задания одной фразой своими словами,
дальше шаги раздела 4: все до единого, по пункту на шаг, ничего не сокращая и не объединяя.
В разделе 4 ровно {{ШАГОВ}} шагов, значит в списке будет {{ШАГОВ+1}} пунктов. Получилось меньше — список
неверный, перепиши его целиком. Последние шаги (гейт и сдача) выкидывать нельзя: пока гейт не
напечатал строку `КОММИТ:`, работа не сделана, чем бы ни кончился твой список дел.
Второе — `git status --short`: пусто — работай, есть чужие изменения — это СТОП (раздел 7).
Третье — своя ветка от `development`: `git checkout -b {{BRANCH}} development`.
Сразу после неё — `git branch --show-current`: в выводе должна быть эта ветка.

## 1. Среда — как пользоваться инструментами

**Главное правило dsh:** не пиши текст перед вызовом инструмента — сразу вызывай инструмент.
Сообщение без вызова инструмента dsh считает концом работы. Текст пишешь один раз — в отчёте из раздела 8.

1. `pwsh` — это Windows PowerShell 5.1. В нём НЕТ: `&&`, `||`, `grep`, `find -name`, `cat`, `sed`,
   `rm -rf`, `del /s /q`. Несколько команд подряд — через `;`.
2. Каждый вызов `pwsh` — новый процесс, `cd` не сохраняется. Нужна другая папка — параметр `workdir`.
3. Искать текст — инструмент `grep`, всегда с `path` (`apps/...` или `packages/...`). Искать файл —
   `glob`. Читать — `read`: файлы длиннее 200 строк — частями через `offset`/`limit`. Менять — `edit`.
   Не читай файл целиком, если нужный кусок уже приведён в задании: чем больше прочитано, тем
   медленнее ты пишешь (20 тыс. токенов истории — 25 слов в секунду, 45 тыс. — уже 12).
4. Всё под `.claude/`, `dist/`, `coverage/`, `node_modules/` — старые копии и сборки. Не читать и не править.
5. В импортах пишется `.js` (`'./x.use-case.js'`), а на диске лежит `x.use-case.ts`. Открывай `.ts`.
6. `edit`: перед каждой правкой перечитай этот кусок файла. `old_string` копируй из вывода `read`
   дословно, без номеров строк. `new_string` обязан отличаться от `old_string`. `replace_all` не используй.
   Один вызов не должен нести больше ~100 строк кода. Пока ты пишешь аргумент, dsh не видит ни
   одного символа и через 20 минут молчания рвёт вызов. Длинный файл создавай в два-три приёма:
   `write` с шапкой и первым тестом, дальше `edit` — по одному тесту.
7. Проверка — только эта команда (в `workdir` = рабочая папка). Свои способы проверки не
   придумывай, кэши не чисти:
   `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/qwen-gate.ps1 -Allowed {{GATE_ALLOWED}} -RequireFile reports/qwen3/tasks/{{ID}}.require.txt`
   В вызове `pwsh` обязательно укажи `timeoutMs: 600000`: полный гейт идёт до 4 минут, а без этого
   dsh обрывает команду через 2.
   Последняя строка вывода — `GATE: PASS` или `GATE: FAIL -> <что упало>`. Над ней — ошибки с `файл(строка)`.
8. git: можно `git status`, `git diff` и создать свою ветку
   (команды — в разделах 0 и 8). Нельзя: `merge`, `rebase`, `push`, `reset`, `stash`, переключаться
   на чужие ветки. Сам `git commit` не запускай: в песочнице dsh он обрывается на хуках husky.
   Коммит делает гейт с `-Commit` (раздел 8), слияние в `development` — координатор.
   git в песочнице dsh иногда печатает `couldn't create signal pipe, Win32 error 5` и отдаёт
   `exit code: 1`, хотя команда выполнена. Это сбой песочницы, а не git: смотри на результат, а
   не на код возврата — после ветки `git branch --show-current`, после коммита `git log --oneline -1`.

## 2. Проверенные факты (коммит `{{HASH}}`, проверено координатором)

### 2.1 Уже есть — используй как есть, заново не создавай
- `{{Символ}}` — `{{путь}}`. Импорт: `{{точная строка import}}`. {{сигнатура или суть}}

### 2.2 Этого НЕТ — не выдумывай, не ищи
- {{поле/метод/класс/файл}} — {{что делать вместо}}

### 2.3 Ловушки — сломается, если не учесть
- {{например: при расширении порта X обнови моки в файлах …}}

## 3. Образцы — копируй структуру, длинные JSDoc не копируй

Комментарий в новом файле — одна-две строки с номером тикета. Сам код бери из образца.

### 3.1 {{Что за образец}} (из `{{путь-образца}}`)
```ts
{{код образца без длинных комментариев}}
```

## 4. План — строго по порядку

Шаг 1. {{действие}} — файл `{{путь}}`. Готово, когда: {{проверяемое условие}}.
Шаг 2. ...
Шаг N. Гейт. Нужен `GATE: PASS`.

После каждого шага, где менялся код, можно запускать гейт. Если он красный — чини только свои
изменения.

## 5. Тесты — что именно должно быть проверено

`{{путь спека}}`:
- {{Given … When … Then …}}
- {{негативный кейс}}

Каждый критерий приёмки тикета — отдельный `it(...)`. Имена тестов на русском, как в соседних спеках.

## 6. Запреты — нарушение = работа не принята

- Не комментируй и не удаляй существующий код, чтобы прошла проверка. Не заменяй `throw` на `return`.
- Не пиши `it.skip`, `@ts-ignore`, `@ts-expect-error`, `as any`. `eslint-disable` — только
  дословная копия уже существующей строки из образца с причиной после `--`.
- Не создавай новые папки, npm-пакеты, README и файлы-отчёты. Не используй express,
  `@nestjs/swagger`, jest.
- `import type` — только для того, что используется исключительно как тип. Класс, декоратор,
  функция, DI-токен → обычный `import`.
- Каждый параметр конструктора Nest-класса — с `@Inject(ТОКЕН или Класс)`. Это проверяет `test:arch`.
- Числа в коде (кроме тестов) — именованные константы (`const MS_PER_MINUTE = 60_000`).
  Функция ≤ 40 строк, файл ≤ 300 строк кода.
- Деньги — целые дирамы, не float. Время в domain и application — только через `Clock`,
  не `new Date()` и не `Date.now()`.
- Упал чужой, как кажется, тест — сначала проверь, не сломало ли его твоё изменение (порт, тип,
  конструктор). Писать «не связано с моими изменениями» можно только после этой проверки.

## 7. СТОП — прекрати работу и сдай отчёт со статусом BLOCKED, если

1. `git status --short` в начале работы не пустой.
2. `grep` не находит символ, который ты собираешься импортировать или вызвать.
3. Нужно изменить файл, которого нет в разделе 0.
4. Гейт падает с одной и той же ошибкой два раза подряд после твоих исправлений.
5. `edit` дважды ответил «file changed since it was read» — файл правит кто-то ещё.
6. Задание противоречит коду: метода, поля или файла нет там, где сказано.
7. В выводе гейта есть «ЭТО НЕ ОШИБКА КОДА: песочница dsh…» — это среда, а не твой код. Код не трогай.

BLOCKED с точной причиной — нормальный результат. Выдуманное DONE — провал.

## 8. Сдача

1. Сдача — одна команда: тот же гейт, что в разделе 1, но с `-Commit`:

```
<команда гейта из раздела 1> -Commit "{{COMMIT_MSG}}"
```

   В вызове `pwsh` обязательно `timeoutMs: 600000`. Гейт сам включает полный режим и коммитит
   ТОЛЬКО при полностью зелёном прогоне, только файлы из `-Allowed` и только в твою ветку.
   Красный гейт — коммита нет: чини ошибки из вывода и запускай ту же команду снова. Сколько
   понадобится раз.
2. Ты закончил тогда и только тогда, когда в выводе есть строка `КОММИТ: <хэш> <сообщение>`,
   а последняя строка — `GATE: PASS`. Нет строки `КОММИТ:` — работа не сдана, что бы тебе ни
   казалось. Не пиши отчёт, пока её нет.
3. Ответ — строго по форме, без пересказа кода:

```
СТАТУС: DONE | BLOCKED
КОММИТ:
<строка КОММИТ: из вывода гейта, дословно>
ФАЙЛЫ:
<строки со статистикой под ней>
ГЕЙТ:
<последние 15 строк вывода гейта, дословно>
КРИТЕРИИ:
<критерий из раздела 5 → файл спека : название it(...)>
ОТКЛОНЕНИЯ: <что сделано не так, как написано, и почему — или «нет»>
БЛОКЕРЫ: <или «нет»>
```

Не пиши «успешно», «всё работает», «соответствует стилю». Не называй файлы, которых не создавал,
и не приводи вывод команд, которых не запускал.

4. Отчёт написан — работа закончена. Цель не закрывай и `update_goal` не вызывай: это делает
   координатор. Дальше ничего не делай и жди.

=== КОНЕЦ ЗАДАНИЯ {{ID}} ===
````

## Зачем каждый блок

| Блок | Что закрывает (номер в ANALYSIS) |
|---|---|
| Маркер конца, «перечитай файл», отправка ссылкой | Вставлен не тот текст (1); обрезание и сжатие контекста (2) |
| Карточка: точный список файлов, цель своими словами в `todo_write`, пустой `git status` | Выдуманная задача (1); выход за периметр; работа поверх чужого (13) |
| «Не пиши текст перед вызовом инструмента» | Вызов без `<tool_call>` → Ollama его не распознаёт, ход обрывается (ANALYSIS §4.7) |
| Запуск через `/goal`, но закрывает цель координатор | `/goal` продолжает работу после обрыва (14). Закрывать цель модели нельзя: сломанный формат вызова превращает автопродолжение в бесконечный цикл (25) |
| Раздел «Среда» | bash в PowerShell (3, 4); `.js` вместо `.ts` (16); старые клоны (17); деструктивные `edit` (10) |
| Одна команда гейта | Пропущенные проверки (15); кэш `.tsbuildinfo`; таблицы покрытия в контексте |
| «Уже есть» с точным импортом | Дубликаты (7); неверные пути импорта |
| «Этого НЕТ» | Выдуманные поля и методы (6); фантомные пакеты (9) |
| «Ловушки» | Сломанные моки чужих спеков (11) |
| Образец того же вида, без JSDoc | express и swagger в контроллере (8); экономия окна 65k |
| План с условием готовности | Модель без собственного «мышления» идёт по внешнему чек-листу |
| Запреты | Закомментированный `throw` (5); `import type` для значений; `@Inject` |
| СТОП-правила | Выход из тупика через BLOCKED, а не через обход проверки (5, 14) |
| Жёсткий формат сдачи | Ложное «успешно выполнил» (14) |
| Коммит делает гейт (`-Commit`) | «Я закончил» на красном гейте и выдуманный хэш (23, 24) |
| Критерии файлом (`-RequireFile`) | Молча пропущенный тест: гейт зелёный, тикет не доделан (26) |
